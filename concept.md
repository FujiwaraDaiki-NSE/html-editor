# html-editor（仮称: Weave）コンセプト整理

作成日: 2026-07-25
更新日: 2026-08-11（プロジェクト思想を明文化）
ステータス: 要件定義の終盤〜基本設計。機能ごとに検討の深さにムラあり（下記3節参照）。

## 1. プロジェクト思想

> **人間の意図とデザインの間にある変換損失をなくす。**
>
> 人の思考を言葉だけに限定せず、曖昧なイメージ、感覚、選択、操作も含めて受け取り、形にしながら意図を発見・更新できる制作環境をつくる。

人の考えは、最初から明確な言葉や要件として存在するとは限らない。曖昧なイメージ、好み、違和感、位置や大きさの感覚、物事の関係性も思考の一部である。一般的なチャット型AIでは、それらを一度プロンプトへ変換してから渡す必要があり、言語化できなかったものはその時点で失われる。

Weaveは、人間の思考をプロンプトという一つの形式に押し込めない。言葉で伝えるだけでなく、対象を指す、形を置く、選ぶ、並べる、比較する、直接直すといった行為も、Agentとの制作に参加できる入力として扱う。目指すのは、表現済みの意図から成果物を生成するだけの環境ではない。人が形をつくり、眺め、試し、修正する過程で、自分の意図そのものを発見し、更新できる環境である。

ラフ、要素選択、直接操作、複数提案と比較は、この思想が現時点で機能として表れたものである。個々の機能が価値なのではなく、**人間の内側にあるものとデザインが接する面を増やすこと**に一貫した価値がある。

新しい機能は、次の問いを基準に設計する。

> その機能は、人間の内側にあるものを、より少ない変換損失でデザインへ伝えられるか。

### 1.1 現在のプロダクト像

品質が高く効率的なスライド作成のため、人間とagentが共同編集できるhtml-editorを作る。

- 人間はウィジェットのようにHTMLで表現可能な要素を追加・削除・ドラッグ&ドロップし、UIで自由にHTMLを扱う
- AgentはコードでHTMLを扱う
- サイドバーにChat UI（agentへの指示）、メインにコードエディタ⇄プレビュー切替、VSCode的な見た目
- Codex app serverを使いChat UI/agent編集体験を実現
- Skillsを共通で使えるようにする
- HTMLは単一ファイルでなく複数ファイルのプロジェクトとして扱える
- 画面はオブジェクトツリーのイメージ

## 2. 決まっている方向性

### 2.1 編集体験の同期
- プレビュー上の要素選択・コード上のカーソル位置・インスペクタが三者同期する
- 人間とagentの編集は同じ履歴（undo/redo）で扱う
- 通常編集は**無条件上書き**（人間が編集中でもagentの変更で保存される）。承認ゲートは設けない
- **コミットのタイミングは明示的な保存ボタンのみ。自動コミットはしない**
  - 保存ボタン＝ディスクへの書き込みとgitコミットを同時に行う1アクション
  - これにより作業ディレクトリは常に「直近のコミットと一致した綺麗な状態」になり、`git checkout`（複数提案のタブ切り替え含む）はgitレベルで衝突が起きず常に安全
  - 保存前のエディタバッファ（メモリ上の未保存編集）は一切保護しない。タブ切り替えやagentの上書きで黙って失われてよい（人間側の責任。警告なし）
  - 保存済み＝コミット済みの状態のみ、git履歴によるundoの対象

### 2.2 Gitを中核の記録システムとする
- プロジェクトごとに自動でgitリポジトリを作成し、ユーザーには一切意識させない
- 用途: (a) undo/変更履歴の実体、(b) 複数提案機能（2.3）のブランチ管理
- ユーザーに見せる「履歴」はraw gitではなく意味単位（1指示=1コミット等）に整えたタイムマシンUIにする想定

### 2.3 複数提案機能（もっとも詳細設計まで進んだ機能）
- Agentは1体、作業ディレクトリも1つ（並列生成・worktreeは不要と判断）
- 案は逐次生成: ベースコミットから案ごとにブランチを切り（例: `weave/variation/a`）、agentがそのブランチ上で編集→コミットを繰り返す
- 閲覧はタブ切り替え: タブクリックで該当ブランチへ`git checkout`。プレビュー/コードエディタは「今チェックアウトされている中身」を表示しているだけなので特別な仕組み不要（2.1の同期の仕組みがそのまま使える）
- 進捗は逐次反映: 各案は「待機中→生成中→完成」の状態を持ち、コミットが完了した時点でタブが即座に完成扱いになる（全案完了を待たない）。最初に完成した案は自動フォーカスするが、閲覧中のタブから勝手にフォーカスは奪わない
- 早期承認時の扱い: 人間が途中の案を選んだら、残りの生成は協調的なキャンセル機構ではなく**agentプロセスの停止**で止める（単一プロセス前提なのでシンプルに済む。gitコミットは原子的なので中断による破損は起きない）
- ブランチのライフサイクル不変条件: 生成された案ブランチは必ず次の3状態のいずれかで終わる
  1. マージされる（選ばれた案）
  2. historyに送られる（完成したが選ばれなかった案）
  3. 破棄される（コミット前にagentが停止された未完成の案）
  → 宙ぶらりんのブランチが残り続けることは原理上起きない

### 2.4 UI設計方向
- VSCode的な4カラム構成: アクティビティレール／サイドバー（チャット専有）／メイン（タブ＋コード⇄プレビュー切替）／インスペクタ、下部にステータスバー
- **スライド一覧はサイドバーに置かない**。チャットは常時読み書きする主機能なのでサイドバーの全高を与え、スライド一覧はキャンバス下のフィルムストリップ（デフォルト）に置く。設定で「サイドバー横の縦レール」に切り替え可能（`weave.slideNav` / localStorage）
  - フィルムストリップが実質ノーコストな理由: 16:9のスライドはキャンバス幅で頭打ちになり縦に余白が残るため、下に76px敷いてもスライドの描画サイズは変わらない。縦レール案は横幅を削るぶんスライドが約7%縮む
- Thread切替・Thread操作（Rename/Fork/Goal/Compact/Archive/Delete）は常設せず、AGENT見出しのドロップダウン内に畳む。会話ログを最優先で広く取るため
- ダーク/ライト両対応。ニュートラルは寒色グラファイト基調、アクセントカラーのみ差し替え可能な設計（プロトタイプで5色: アンバー/ティール/バイオレット/コーラル/セージ）
- ステータスバーは「history」表記にして、裏側がgitであることをユーザーに感じさせない
- プレビュー内要素クリック→コード/インスペクタ連動、複数提案のタブ選択UIを含む動作モックアップを作成済み

### 2.5 Agentへの自動コンテキスト
- 保存前のバッファは保護しない（2.1）が、agentは編集前に**今の状態を必ず見る**ことでリスクを事前に減らす。コードエージェントが未保存のエディタ内容を見るのと同じ発想
- 渡すコンテキストは3層で整理:
  1. **バッファ状態**: 開いているファイルの中身（未保存でも）、カーソル位置・選択範囲
  2. **選択/フォーカス状態（最優先・必須）**: プレビュー/ツリーで今選択中の要素、今アクティブなタブ/スライド。「ここ直して」の"ここ"を解決するのに必須で、これが無いと成立しない
  3. **プロジェクトレベル**: 共有Skills、直近のコミット履歴の要約、デッキ内の他スライド（一貫性のため）

### 2.6 直接操作（ドラッグ&ドロップ）
- **自由配置は採用しない**。レイアウトはフロー的（flex/grid）を維持する。レイアウトを綺麗に保つのが目的
- 操作感は自由配置キャンバスというより**ブロックエディタ型**（Notion/Gutenberg的）: ドラッグハンドルで掴む、挿入位置をインジケータで示す、コンテナ内外への並び替え・移動
- 要素を動かした際に残りが自動でいい感じに整列するのは、新しい仕組みではなくflex/grid+gapベースでCSSを書くことで標準機能として得られる。これは**agentが従うべきCSS記述ルール**として2.5のSkillsに組み込む（固定px指定は避け、flex/gridを使う）
- インスペクタはX/Y/W/Hの直接指定ではなく、**タグ種類ごとのメタデータスキーマ**から自動生成されるプロパティパネルにする（例: テキスト系→配置/太さ/色、コンテナ系→並び方向/揃え/gap、画像→トリミング/角丸）。以前のモックアップのX/Y/W/H欄はこの方針に合わせて作り直しが必要
- タグ/ウィジェットの種類レジストリは、**一旦は固定・キュレーション済みのセット**とする（人間・agentによる拡張は今回は見送り）
- コンテナ（div等）は**人間が自由に追加できる**。ただし生のdivではなく、レジストリに含まれる決まった種類（行/列/グリッドボックスなど、gap等のデフォルト値込み）から選ぶ形にする。これにより自由な構造の組み立てと、デッキ全体のレイアウトの一貫性を両立させる

### 2.7 スライド背景（テーマ）
- コンテンツ（2.6の自由に組み立てるコンテナ群）とは別に、常に最背面に敷かれる**独立した背景レイヤー**を持つ
- ロゴ・装飾・配色を持ち、ドラッグ&ドロップ編集の対象外（オブジェクトツリーにも出さない想定）
- 背景が定義する配色（CSS変数）を2.6のウィジェットレジストリ側が参照する形にすれば、背景を切り替えるとコンテンツ側の見た目も連動して変わる（テーマ切り替えに近い）
- **1つのデッキ内で複数の背景を使い分けられる**（表紙用・本文用など、スライドごとに選択）
- そのプロジェクト内では背景自体は**編集不可**。用意された背景から選ぶのみ
  - 未決: 背景テンプレート自体はどこで作られる/供給されるのか（Skills共有の仕組みと同様、プロジェクト外で管理される想定だが詳細は未定）
- **GUIから選択でき、スライド単位でいつでも変更できる**（作成時だけでなく後からも切り替え可能）。背景とコンテンツが独立レイヤーなので、切り替えてもコンテンツ構造には影響しない
  - UIイメージ: モックアップの配色スウォッチ（キャンバス左下）と同じ位置に、背景のサムネイル選択を置く想定

### 2.8 Codex app serverとの統合（認証まわりを含め、対応範囲は広く取る）
公式ドキュメント（learn.chatgpt.com/docs/app-server, /auth, github.com/openai/codex）で確認済み。認証は2つのレイヤーに分かれる。

移行は完了済み。旧Chatデータと旧実装は保持せず、app-server Threadが会話の唯一の正規データ源になっている
（[リリースノート](./docs/release-notes.md)参照）。

- **レイヤー1: html-editorの自作UI ⇄ ローカルのCodex app-serverプロセス**
  - JSON-RPC 2.0、stdio/WebSocket/Unixソケット経由
  - WebSocket認証: capability token（ファイルベース共有シークレット）／signed bearer token（HMAC、本番向け）／開発時のみunauthenticated localhost
  - 自分で立てたプロセスに自分で繋ぐだけなので実装難易度は低い
- **レイヤー2: Codex ⇄ OpenAI本体（対応範囲を広く取りたい本丸）**
  - ChatGPTアカウントログイン（OAuthブラウザフロー、個人サブスクリプション）
  - APIキー（従量課金、開発者向け）
  - デバイスコードフロー（ローカルブラウザコールバックが使えない環境向け）
  - アクセストークン（Enterprise workspace、非対話的自動化向け）
  - → 個人のChatGPTログイン・APIキー開発者・Enterpriseのアクセストークン運用まで、すべてカバーする方針
  - 資格情報は`~/.codex/auth.json`にキャッシュ・自動更新される（平文/OS資格情報ストアを設定可）。Codex CLIを子プロセスとして起動するなら、この既存の認証フローにそのまま乗れる可能性が高く、html-editor側で作り込むのは主にログイン状態の表示・促しのUIで済む見込み

**app-serverの標準機能で、既存の設計とそのまま噛み合うもの**
- Skillベースのプロンプティング → 2.5のSkills共有の仕組みはこれに乗せられる
- ターン単位のストリーミングイベント（ファイル変更/ツール呼び出し/進捗の逐次通知） → 2.3複数提案機能の「逐次完成をタブに反映」とそのまま対応
- サンドボックスポリシー（read-only/workspace-write/unrestricted等） → agent生成HTML/JS実行のセキュリティ対策に使える
- 承認ワークフロー（コマンド実行・ファイル変更の承認）がプリミティブとして存在 → 2.1で「無条件上書き」を選んだため今回は使わない想定だが、サーバー側の機能としては利用可能

### 2.9 スタイルの単一ソース化と固定設計サイズ（2026-08-01決定・実装済み）
プレビュー用CSS（アプリ側）と書き出しHTML用CSS（サーバ側テンプレート）が独立に手書きされ、既に食い違っていた問題への対応。

- **スライドの見た目を定義する場所はプロジェクト内の`styles/deck.css`ただ1つ**。プレビューはこのCSSをそのまま読み込み、書き出しHTMLには同じ内容をインライン化する（単体で開けるファイルであることは維持）
- **設計サイズを1280×720に固定**し、CSSは絶対pxで書く。表示側は全体を`transform: scale()`で縮小して収める
  - これにより1本のCSSがプレビュー・書き出し・将来のサムネイルで同一の見た目になる（実測でピクセル一致を確認）
  - 副作用としてズームUIが実機能になり、`clamp()`等のレスポンシブ指定が全て不要になった
- **セレクタは必ず`.weave-slide`配下に書く**ルールとし、iframe/Shadow DOMによる隔離は採用しない。隔離のためにプレビューの選択・ドラッグ・直接編集を座標変換越しに作り直すコストを避けるため
- スライドは「文書のルート」として自分自身のタイポグラフィを宣言する（エディタUIのフォント設定を継承しない）
- マークアップ生成も`shared/slide-design.mjs`に一本化し、コードビューは捏造したHTMLではなく**次の保存で書き出される実物**を表示する
- `slides/*.html`は`deck.json` + `deck.css`からの生成物。**`deck.css`は再生成の対象外**とし、agentが書いた内容が保存で失われないようにする
- 未実装: インスペクタからの見た目変更。生CSSは人間に触らせず、レジストリのメタデータスキーマ（2.6）から生成したUIでトークンを選ぶ形にする。そのためのトークン保存先を`deck.json`のブロックに追加する必要がある
  - → **2.10で撤回**。トークン方式そのものをやめ、HTMLを単一の真実とする方向に転換した

### 2.10 データモデルの再設計：HTMLを単一の真実とする（2026-08-02決定。2.6のトークン方式インスペクタと2.9後半を置き換え）
CSS/サイズ編集が実質機能していない問題を掘り下げた結果、原因はデータモデルの過剰抽象化だと判断した。

**問題（現状の3表現）**
- 同じスライドを3つの表現で二重・三重に保持していた:
  1. `deck.json`の`Block[]`＋スタイルトークン（`size: sm/md/lg`等）＝「正規のエディタ状態」
  2. そこから生成される`slides/*.html`（成果物・直接編集禁止）
  3. `styles/deck.css`（手書き/agent所有、トークン語彙と手作業で整合）
- トークン語彙に無いものは**原理的に表現不可能**（任意のpx指定ができない）＝サイズが弱い正体
- 真実が3か所に分散し、実プロジェクトの`deck.css`からトークンクラスが消えても誰も気づかない**ドリフト**が発生していた（インスペクタのSize変更が無反応）
- 抽象層のために`slide-design.mjs`の二重描画、`validateDeck`のトークン白名簿の再実装、保存時のHTML再生成トランザクションと、複雑さが積み上がっていた。しかもトークンは**agentの得意分野（生HTML/CSS記述）を狭いJSONスキーマに閉じ込めていた**

**決定**
- 真実を`slides/*.html` + `styles/deck.css`（実ファイル）に一本化する。`deck.json`をトークンによる正規状態の座から降ろす
- 人間はライブDOM上で編集（contenteditable/選択/ドラッグ）し、保存時にDOM→HTMLで同じファイルへ書き戻す。agentは`.html`/`.css`を直接編集する（得意分野）
- インスペクタは3段トークンではなく、**要素への実CSS（インラインstyle/クラス）と種別ごとの意図**（テキスト→font-size等、コンテナ→並び方向/gap/揃え/子の伸縮）。指定しないサイズは**計算値のまま**残す
  - → 意図の持ち方とUIへの置き方は**2.11**で具体化した
- 撤去対象: `slide-design.mjs`の二重描画、`validateDeck`のトークン白名簿、token→class対応、保存時HTML再生成トランザクションの大半
- レイアウト一貫性は旧トークンの「スキーマ強制」から、**Skill/lintでの担保**に移す（`content-policy.mjs`の素地を活用）

**維持（2.9の前半はそのまま活きる）**
- 単一`deck.css`・固定設計サイズ1280×720・絶対px・scale-to-fit・全セレクタ`.weave-slide`配下。これらは方向1でも有効。必要なら設計サイズをデッキ設定に

**根拠：宣言的スタイル＋固定キャンバスは対立しない**
- スライドは固定出力媒体（PDF/PPTX/投影＝1つの固定矩形）。固定設計サイズは媒体と一致し、preview≡export≡thumbnailのピクセル一致・座標変換の単純化・レスポンシブ単位不要・**agentが出力を正確に予測して書ける**・WYSIWYG忠実・決定的なdiffを生む
- 宣言的フローは固定フレームの中でも「編集で自動整列」「一貫性」「agentの寛容な記述」を提供する。固定キャンバスが捨てたのは「多様なビューポートへの適応」だけで、スライドには元々不要
- PPT式の記述的（絶対配置）ではなく宣言的を選ぶ核心理由は、agentにとってフローが**寛容**（重なり・不整列をエンジンが防ぐ）で、絶対配置が**非寛容**（1つ数字を誤ると衝突）だから。人間＋agent共同編集ではこの寛容さが決定的

### 2.11 インスペクタの設定の置き方（2026-08-07決定・実装済み）
2.10で「インスペクタは実CSSと種別ごとの意図」と決めたが、その**意図をどう持ち、UIのどこに置くか**は未定だった。実際に触ると「コンテナ自体を中央寄せできない」という形で詰まったため、そこから整理した。

**問題**
- Tailwind移行時（2.9→2.10）に旧CSSの`.weave-container { width: 100% }`を`w-full`としてクラス列に引き写した結果、**全コンテナが常に幅いっぱい**になっていた。幅いっぱいのものは動かす余地がないので、どんな揃えクラスを足しても中央寄せは原理的に不可能だった。しかも`w-full`はRow内で`flex-1`の代用として機能してしまい、意味の違うクラスが正しい見た目を出す状態だった
- 設定が「箱を動かすもの」「中身を動かすもの」「他の設定の詳細」を区別せず1列に並び、`text-align`（文字）・`align-self`（自分）・`align-items`（子）が同じ重みで見えていた

**決定A: サイズは意図（Fill / Hug / Fixed）で持つ**
- 幅はRowでは主軸、Columnでは交差軸。**同じ意図が親によって別クラスになる**（`flex-1`/`flex-none` ⇄ `self-*`）ので、対応付けは編集側が親の軸を見て解決する。ユーザーにflexの軸を考えさせない（エディタである以上、これはツールの仕事）
- gridセルは自前のサイズクラスを持たない（親のテンプレートが幅を決めるため）
- **親が変わる操作でのみ書き直す**。ドラッグやDirection反転で軸をまたぐときは意図を保って別クラスに置換し、同じ親の中での並べ替えでは何も書かない（保存HTMLとdiffを汚さないため）
- **往復不変を保証する**。`applySize`した結果を`readSize`すると必ず同じ意図が返る。これが崩れると「押しても効かないボタン」が生まれる（実際に生まれた）。`shared/tailwind-slide.mjs`の2関数が単一の実装で、テストで3意図×2軸×初期状態を網羅
- 実効的な`max-w-*`は`flex-1`/`self-stretch`より**優先して**意図を決める。キャップがあれば箱はそこで止まるので、それが描画の実態

**決定B: 設定の置き方は粒度3段**
1. **共通**（全ブロックが持つ）: Width / Position → `BLOCK`セクション
2. **種類特有の基本**: `TEXT`（Size/Weight/Leading/Align/Color）または`CONTAINER`（Direction/Gap/Padding/Justify/Align items）
3. **詳細**（ある選択で初めて意味を持つ）: 独立セクションを作らず、**トリガーの直下にインラインで出す**。Measureは`Width = Fixed`のときだけ
   - 詳細を`ADVANCED`として1セクションにまとめる案は一度実装して撤回した。トリガーが視界から外れると「なぜ今これが出たか」が読めないため

**決定C: 名前は対象で分ける**
- **Width**（箱の幅）/ **Position**（箱の位置＝`align-self`）/ **Align**（文字＝`text-align`）/ **Align items**（子＝`align-items`）
- 「箱を中央に置く」が`text-align`では到達できないことが最頻の詰まりだったので、ここは名前で必ず区別する
- **Measure**は組版用語（1行の長さ）。実効的な`max-w-*`を持つことが`Fixed`の定義そのものなので、Fixedは既定値を伴って書かれ、Fill/Hugは既存のキャップを落とす

**決定D: 旧クラスは遅延移行**
- `w-full`は読みでFillと解釈し、サイズを書き込むときに落とす。一括マイグレーションはしない（既存デッキの見た目を変えるリスクを負わずに済み、編集した分だけ正しい表現に移る）

**残っている制約**
- gridセルにはMeasureを付ける手段がない（Width行自体を出さないため）。必要になったら戻す
- `metrics`ブロックのDirectionが`display`を壊す既存不具合（本決定以前から）。TASK.md参照

## 3. まだアイデア段階（深掘りしていない）

- 複数ファイルプロジェクトの構成・管理方法
- Skillsの共有の仕組み（プロジェクト固有の「良い作り方」をagentに教える方法。2.6のCSS記述ルールもここに含まれる）
- プレゼンモード、PDF/PPTXエクスポート
- Agent生成物の品質チェック（アクセシビリティ、ビジュアルリグレッション）
- 複数人間×複数agentの同時編集
- Codex app serverが複数提案機能の前提（途中停止・ステップ単位の進捗通知）を実際にサポートするかの確認

## 4. 検討スタイル上のメモ

複数提案機能の設計は「まず作り込む案→無駄を削って単純化する案」を繰り返して収束させた:
画像サムネイル→実HTMLのライブiframe→タブ切り替え、並列worktree→単一agent逐次生成、
協調的キャンセル→プロセス停止。効率・無駄のなさを優先する判断が一貫している。
