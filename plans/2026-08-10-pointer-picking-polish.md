# Pointer picking polish

1. Keep pointer tabs persistent only for nested candidates and existing references; show leaf tabs on frame hover.
2. Remove pointer tabs from keyboard and assistive-technology navigation while retaining the frame as the control.
3. Preserve `<br>` boundaries as whitespace in element excerpts without using `innerText`, with unit coverage for the pure helper.
4. Add inline breathing room around the selected-element reference button.
5. Run lint and the full test suite, then report the four focused commits without merging or pushing.
